version: '3'
services:
    volume-example:
        container_name: volume-example
        build:
            dockerfile: Dockerfile
            context: ./
        volumes:
            - myApp:/myFolder
volumes:
    myApp: